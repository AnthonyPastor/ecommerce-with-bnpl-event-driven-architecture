{
  "extends": "next/core-web-vitals",
  "rules": {
    "no-restricted-imports": [
      "error",
      { "patterns": [{ "group": ["../*"], "message": "Use an @alias import (@app, @components, @lib, @store) instead of a relative parent import." }] }
    ]
  }
}
